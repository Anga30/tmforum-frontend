export function IndividualDetailsFields() {
  return <fieldset className="field-group"><legend>Individual details</legend><p className="field-help">These details belong to the Individual Party created for this administrator.</p><div className="field-grid"><label>First name <span aria-hidden="true">*</span><input name="givenName" required maxLength={120} autoComplete="given-name" /></label><label>Middle name <span className="optional">Optional</span><input name="middleName" maxLength={120} autoComplete="additional-name" /></label><label>Surname <span aria-hidden="true">*</span><input name="familyName" required maxLength={120} autoComplete="family-name" /></label><label>Date of birth <span className="optional">Optional</span><input name="birthDate" type="date" /></label><label>Gender <span className="optional">Optional</span><input name="gender" maxLength={50} /></label><label>Nationality <span className="optional">Optional</span><input name="nationality" maxLength={100} /></label></div></fieldset>;
}
